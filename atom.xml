<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://daning.netlify.app</id>
    <title>鼓捣猫宁</title>
    <updated>2020-09-10T04:42:06.416Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://daning.netlify.app"/>
    <link rel="self" href="https://daning.netlify.app/atom.xml"/>
    <subtitle>梧高凤必至，花香蝶自来</subtitle>
    <logo>https://daning.netlify.app/images/avatar.png</logo>
    <icon>https://daning.netlify.app/favicon.ico</icon>
    <rights>All rights reserved 2020, 鼓捣猫宁</rights>
    <entry>
        <title type="html"><![CDATA[正则相关]]></title>
        <id>https://daning.netlify.app/post/regexp-rule/</id>
        <link href="https://daning.netlify.app/post/regexp-rule/">
        </link>
        <updated>2020-09-08T09:01:59.000Z</updated>
        <summary type="html"><![CDATA[<p>对正则了解不多，在此记录一下平时经常用到的表达式&amp;修饰符</p>
]]></summary>
        <content type="html"><![CDATA[<p>对正则了解不多，在此记录一下平时经常用到的表达式&amp;修饰符</p>
<!-- more -->
<h2 id="1-匹配规则">1 匹配规则</h2>
<p>参考文章：<a href="https://www.runoob.com/regexp/regexp-rule.html">正则表达式 - 匹配规则</a><br>
例：校验字符串中是否包含 ':', ';', '/', '', '%', '`'</p>
<pre><code class="language-objectivec">NSString *nameRegex = @&quot;^[^:;\\\\/%`]+$&quot;;
NSPredicate *namePredicate = [NSPredicate predicateWithFormat:@&quot;SELF MATCHES %@&quot;,nameRegex];
return [namePredicate evaluateWithObject:name];
</code></pre>
<h3 id="11-基本匹配模式">1.1 基本匹配模式</h3>
<ul>
<li>
<p><code>$</code>符号<br>
表示<code>结尾</code>：例如<code>bucket$</code>，匹配‘in a bucket’，而不匹配‘buckets’</p>
</li>
<li>
<p><code>^</code>符号<br>
有2个含义：</p>
<ol>
<li>&quot;开头&quot;，以<code>^abc</code>为例：匹配以<code>abc</code>开头的字符串</li>
<li>&quot;非&quot;或&quot;排除&quot;，以<code>^[^abc]+$</code>为例：从<code>头</code>到<code>尾</code>，不包含（排除）<code>a、b、c</code>的字符串</li>
</ol>
</li>
</ul>
<h3 id="12-字符簇">1.2 字符簇</h3>
<ul>
<li>简单说就是用来描述我们匹配模式的方法，书写方式为<code>[.....]</code><pre><code>[a-z] //匹配所有的小写字母 
[A-Z] //匹配所有的大写字母 
[a-zA-Z] //匹配所有的字母 
[0-9] //匹配所有的数字 
[0-9\.\-] //匹配所有的数字，句号和减号 
[ \f\r\t\n] //匹配所有的白字符
[^a-z] //除了小写字母以外的所有字符 
[^\\\/\^] //除了(\)(/)(^)之外的所有字符 
[^\&quot;\'] //除了双引号(&quot;)和单引号(')之外的所有字符
</code></pre>
</li>
<li>举个栗子🌰，匹配一个小写字母和一个数字组成的字符串，如‘a1’、‘b2’，但不是&quot;ab2&quot;：<pre><code>^[a-z][0-9]$
</code></pre>
</li>
<li>再举个栗子🌰，同样匹配2个字符的字符串，第一个不是数字，第二个是数字，如‘#1’、‘R2’：<pre><code>^[^0-9][0-9]$
</code></pre>
</li>
</ul>
<h2 id="2-元字符">2 元字符</h2>
<p>参考文章：<a href="https://www.runoob.com/regexp/regexp-metachar.html">正则表达式 - 元字符</a><br>
这里组合演示了：</p>
<ul>
<li>
<p><code>?&lt;=</code> : xx之后<br>
例：?&lt;=href 表示 匹配 href之后  (不包含href)</p>
</li>
<li>
<p><code>?=</code> : xx之前<br>
例： ?=href 表示 匹配 href之前  (包含href)</p>
</li>
<li>
<p><code>.*?</code> : 取尽量少的任意字符</p>
<blockquote>
<p>注：<code>.</code>代表的是任意字符。<code>*</code> 代表的是取 0 至 无限长度。<code>？</code>代表的是非贪婪模式。</p>
</blockquote>
<p>例子，<code>CFBundleDisplayName=&quot;MUZO Player&quot;;</code>字符串中，我想获取<code>MUZO Player</code>:</p>
<pre><code>(?&lt;=CFBundleDisplayName=&quot;).*?(?=&quot;)
</code></pre>
</li>
<li>
<p><code>?&lt;!</code> : 不以xx开头<br>
例子，遍历字符串文件时，想过滤掉 <code>//&quot;adddevice_test&quot;=&quot;test&quot;;</code>这种字串：</p>
<pre><code>// 不以(//)开头，并且格式为(&quot;xxx&quot;可能存在空格=可能存在空格&quot;xxx&quot;;)
(?&lt;!//)&quot;.*?&quot; ?= ?”.*?”;  
</code></pre>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用 Gridea+Github Pages 搭建个人博客]]></title>
        <id>https://daning.netlify.app/post/create-blog-with-gridea/</id>
        <link href="https://daning.netlify.app/post/create-blog-with-gridea/">
        </link>
        <updated>2020-09-07T09:36:33.000Z</updated>
        <summary type="html"><![CDATA[<p>Gridea 是一个静态博客写作客户端，它对比Jekyll、Hexo搭建博客的优势就在于拥有一个独有的客户端，不需要把注意力放在UI的配置上，只需要进行简单的配置即可迅速上手搭建个人博客！</p>
]]></summary>
        <content type="html"><![CDATA[<p>Gridea 是一个静态博客写作客户端，它对比Jekyll、Hexo搭建博客的优势就在于拥有一个独有的客户端，不需要把注意力放在UI的配置上，只需要进行简单的配置即可迅速上手搭建个人博客！</p>
<!-- more -->
<h2 id="github-pages简介">Github Pages简介</h2>
<p><a href="https://pages.github.com/">Github Pages官网</a></p>
<p>Github Pages是一个静态站点托管服务，也就是说你完全可以随便做一个静态网页，来展示你要的东西，最重要的一点，它是免费的！😏</p>
<p>Github Pages也有自己的主题，如果不依赖其他工具去搭建blog也是可以的，<a href="https://sspai.com/post/54608">GitHub Pages 搭建教程</a>，没尝试过，不做评价；</p>
<h2 id="gridea">Gridea</h2>
<p>官网：<a href="https://gridea.dev/">Gridea官网</a></p>
<p>主题：<a href="https://gridea.dev/themes/">Gridea主题</a></p>
<h2 id="步骤">步骤</h2>
<ul>
<li>
<p>详细过程：<a href="https://gridea.dev/gridea-start/">官方教程</a></p>
</li>
<li>
<p>大体过程：</p>
<ol>
<li>下载<a href="https://gridea.dev/#started">Gridea客户端</a></li>
<li>创建Github Token，路径：Setting -&gt; Developer settings -&gt; Personal access tokens -&gt; Generate new token; 创建完成后记得保存，如果退出当前页面，token就再也看不到了！</li>
<li>创建仓库，并在客户端里配置Github信息；</li>
</ol>
</li>
</ul>
<h2 id="netlify加速">Netlify加速</h2>
<p>如果需要使用Netlify的CDN加速，并免费自定义域名<br>
可以参考：<a href="https://daning.netlify.app/post/howtousenetlify/">使用Netlify加速Github Pages访问速度(Jekyll)</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Mac监听键盘点击]]></title>
        <id>https://daning.netlify.app/post/listen-keyboard-event/</id>
        <link href="https://daning.netlify.app/post/listen-keyboard-event/">
        </link>
        <updated>2020-08-28T09:32:06.000Z</updated>
        <summary type="html"><![CDATA[<p>总结了Swift中，暂时发现的3种键盘(鼠标)点击的监听方式</p>
]]></summary>
        <content type="html"><![CDATA[<p>总结了Swift中，暂时发现的3种键盘(鼠标)点击的监听方式</p>
<!-- more -->
<blockquote>
<p>注意，以下方法中，涉及到全局键盘监听的都需要系统提供权限才能正常使用，判断方式：</p>
</blockquote>
<pre><code class="language-swift">//系统首选项&gt;安全&amp;隐私权&gt;隐私权&gt;辅助功能 - 请确保您的应用已被选中。
print(&quot;辅助功能:&quot;,AXIsProcessTrusted() ? &quot;信任&quot; : &quot;不信任&quot;)
</code></pre>
<h1 id="一-系统自带的api">一、系统自带的API</h1>
<blockquote>
<p>其实这两个监听就可以满足大部分的需求了，唯一的不足是像F7~F9的媒体功能键无法监听</p>
</blockquote>
<ul>
<li>Global监听</li>
</ul>
<pre><code class="language-swift">    NSEvent.addGlobalMonitorForEvents(matching: [.keyDown]) { [unowned self] (event) in
        // flags貌似是获取与常规键无关的按键
        let flags = event.modifierFlags.intersection(.deviceIndependentFlagsMask)
        // 常规 option || 带有左右键时的 cmd+option
        if (flags == NSEvent.ModifierFlags.option || flags.contains([.command,.option])) {
            self.playControlWithKeyCode(event.keyCode)
        }
    }
</code></pre>
<ul>
<li>Local监听</li>
</ul>
<pre><code class="language-swift">    NSEvent.addLocalMonitorForEvents(matching: [.keyDown]) { [unowned self] (event) -&gt; NSEvent?  in
        if self.TextFieldIsEditing == true { return event}
        let code = event.keyCode
        let flags = event.modifierFlags.intersection(.deviceIndependentFlagsMask)
        
        if (flags.contains([.option])) { // 包含option
            self.playControlWithKeyCode(code)
        } else {
            if flags == NSEvent.ModifierFlags.command {
                if code == kVK_ANSI_W {
                    WindowManager.share.currentWindow?.close()
                }
            } else {
                if (code == kVK_ANSI_W || code == kVK_Space){
                    self.playControlWithKeyCode(code)
                }
            }
        }
        return nil
    }
</code></pre>
<h1 id="二-子类化nsapplication">二、子类化NSApplication</h1>
<p>我们可以通过子类化NSApplication，拦截sendEvent来实现获取键盘鼠标的点击事件，就像注释中描述的，虽然可以达到获取媒体键的点击，但是却无法拦截唤起的iTunes，不过用来获取其他按钮的点击事件也是可以的</p>
<blockquote>
<p>注意：子类化NSApplication，需更改Info.plist里的NSPrincipalClass，如果是Swift则设置为$(PRODUCT_MODULE_NAME).MyApplication才会有效，如果是OC则直接设置为MyApplication即可，当然类名可以自己随意定义，我这里仅仅举个例子</p>
</blockquote>
<pre><code class="language-swift">class MyApplication: NSApplication {
    // 可以通过重写sendEvent监听媒体功能键，但是会唤起iTunes音乐，无法拦截，所以放弃
    override func sendEvent(_ event: NSEvent) {
        if  event.type == .systemDefined &amp;&amp;
            event.subtype == .screenChanged
        {
            let keyCode : Int32 = (Int32((event.data1 &amp; 0xFFFF0000) &gt;&gt; 16))
            let keyFlags = (event.data1 &amp; 0x0000FFFF)
            let keyState = ((keyFlags &amp; 0xFF00) &gt;&gt; 8) == 0xA

            mediaKeyEvent(withKeyCode: keyCode, andState: keyState)
            return
        }
        super.sendEvent(event)
    }
}
</code></pre>
<h1 id="三-cgeventtapcreate拦截">三、CGEvent.tapCreate拦截</h1>
<blockquote>
<p>此方法一样可以直接获取键盘事件，比子类化NSApplication高级的是可以拦截事件，例如拦截媒体键对iTunes的控制，不废话，直接上代码</p>
<blockquote>
<p>同时，有趣的是这种方式，在首次加载时，会弹出系统提示框，获取隐私服务权限，比较友好</p>
</blockquote>
</blockquote>
<pre><code class="language-swift">// 这个方法也可以拿来监听键盘其他key的点击
    func startListenCGEventTap() {
        func myCGEventCallback(proxy: CGEventTapProxy, type: CGEventType, event: CGEvent, refcon: UnsafeMutableRawPointer?) -&gt; Unmanaged&lt;CGEvent&gt;? {
            if(type == .tapDisabledByTimeout){
                return Unmanaged.passRetained(event)
            }
            
            if let nsEvent = NSEvent.init(cgEvent: event) {
                if (nsEvent.type == .systemDefined &amp;&amp; nsEvent.subtype == .screenChanged ) {
                    let keyCode : Int32 = (Int32((nsEvent.data1 &amp; 0xFFFF0000) &gt;&gt; 16))
                    let keyFlags = (nsEvent.data1 &amp; 0x0000FFFF)
                    let keyState = ((keyFlags &amp; 0xFF00) &gt;&gt; 8) == 0xA
//                    let keyIsRepeat = (keyFlags &amp; 0x1) &gt; 0
//                    if keyIsRepeat {
//                        print(&quot;keyIsRepeat&quot;)
//                        return nil
//                    }
                    mediaKeyEvent(withKeyCode: keyCode, andState: keyState)
                    return nil
                }
            }

            return Unmanaged.passRetained(event)
        }
        
        // let eventMask = (1 &lt;&lt; CGEventType.keyDown.rawValue) | (1 &lt;&lt; CGEventType.keyUp.rawValue | (1 &lt;&lt; CGEventType.flagsChanged.rawValue))
        
        // 创建eventTap，此时会唤起’打开隐私权限‘弹窗
        if let eventTap = CGEvent.tapCreate(tap: .cgSessionEventTap,
                                            place: .headInsertEventTap,
                                            options: .defaultTap,
                                            eventsOfInterest: CGEventMask(NX_SYSDEFINEDMASK),
                                            callback: myCGEventCallback,
                                            userInfo: nil) {
            let runLoopSource = CFMachPortCreateRunLoopSource(kCFAllocatorSystemDefault, eventTap, 0)
            CFRunLoopAddSource(CFRunLoopGetCurrent(), runLoopSource, .commonModes)
            CGEvent.tapEnable(tap: eventTap, enable: true)
            CFRunLoopRun()
        } else {
            print(&quot;failed to create event tap&quot;)
//            exit(1)
        }
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NSMenu右键后，或者拖入文件时，cell的边框和下划线处理]]></title>
        <id>https://daning.netlify.app/post/table-view-menu-right-click/</id>
        <link href="https://daning.netlify.app/post/table-view-menu-right-click/">
        </link>
        <updated>2020-07-24T09:31:23.000Z</updated>
        <summary type="html"><![CDATA[<p>我们在配合使用 NSTableView/NSOutlineView 和 NSmenu 的时候，会发现右键cell时，会多出一个蓝色的边框;<br><br>
拖入文件到tableview里时，cell上也会多出一个下划线;<br><br>
这里介绍如何拦截并修改它们</p>
]]></summary>
        <content type="html"><![CDATA[<p>我们在配合使用 NSTableView/NSOutlineView 和 NSmenu 的时候，会发现右键cell时，会多出一个蓝色的边框;<br><br>
拖入文件到tableview里时，cell上也会多出一个下划线;<br><br>
这里介绍如何拦截并修改它们</p>
<!-- more -->
<h1 id="一-右键cell时的边框">一、右键cell时的边框</h1>
<h2 id="分析">分析</h2>
<ul>
<li>
<p>右键cell后，边框的样子如下，很突兀:<br>
<img src="https://s1.ax1x.com/2020/09/07/wuIScF.png" alt="示例" loading="lazy"></p>
</li>
<li>
<p>实际上，就是在tableView上 addSubview 了一个 NSMenuHighlightView：<br>
<img src="https://s1.ax1x.com/2020/09/07/wuIpX4.png" alt="示例" loading="lazy"></p>
</li>
</ul>
<h2 id="解决方式">解决方式</h2>
<blockquote>
<p>暴力点，直接重写NSTableView的addSubview方法，拦截掉NSMenuHighlightView的添加:</p>
</blockquote>
<pre><code class="language-swift">class DNTableView: NSTableView {
    // 禁止右键 cell 边框
    override func addSubview(_ view: NSView) {
        if view.className != &quot;NSMenuHighlightView&quot; {
            super.addSubview(view)
        }
    }
}
</code></pre>
<h1 id="二-拖入文件时的下划线">二、拖入文件时的下划线</h1>
<blockquote>
<p>当然也可以通过设置 <code>tableView.draggingDestinationFeedbackStyle = .none</code> 直接隐藏，但是我更想修改它的颜色</p>
</blockquote>
<ul>
<li>
<p>首先看看它的样子<br>
<img src="https://s1.ax1x.com/2020/09/07/wuIpX4.png" alt="示例图" loading="lazy"></p>
</li>
<li>
<p>其实原理和上面边框处理一样，也是通过重写addSubView进行拦截</p>
</li>
</ul>
<pre><code class="language-swift">// 禁止右键cell 边框
    override func addSubview(_ view: NSView) {
        if view.className == &quot;NSMenuHighlightView&quot; { // 右键后cell上的线
            return
        } else if view.className == &quot;NSDraggingDestinationView&quot; { // 拖动文件后，cell上的线
            // 发现无法修改view的背景色，无奈只能覆盖一个color view，以后发现更好的修改方式再改
            let newLineView = NSView()
            newLineView.frame = view.bounds
            newLineView.wantsLayer = true
            newLineView.layer?.backgroundColor = kRedHighlightColor.cgColor
            view.addSubview(newLineView)
            super.addSubview(view)
        } else {
            super.addSubview(view)
        }
    }
</code></pre>
<ul>
<li>再来看看修改后的样子<br>
<img src="https://s1.ax1x.com/2020/09/07/wuIi7R.png" alt="示例图" loading="lazy"></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[自定义NSSlider]]></title>
        <id>https://daning.netlify.app/post/custom-nsslider/</id>
        <link href="https://daning.netlify.app/post/custom-nsslider/">
        </link>
        <updated>2020-07-22T09:30:16.000Z</updated>
        <summary type="html"><![CDATA[<p>见名知意，本文介绍如何自定义NSSlider</p>
]]></summary>
        <content type="html"><![CDATA[<p>见名知意，本文介绍如何自定义NSSlider</p>
<!-- more -->
<blockquote>
<p>NSSlider由2部分组成，外层的<code>NSSlider</code>以及内部的<code>NSSliderCell</code>。通常我们所说的自定义Slider说的就是自定义<code>NSSliderCell</code>。</p>
</blockquote>
<h2 id="自定义nsslidercell">自定义NSSliderCell</h2>
<p>以我自定定义的DNSliderCell举例</p>
<h3 id="1-定义实现代理">1. 定义/实现代理</h3>
<blockquote>
<p>NSSliderCell继承自NSActionCell，NSActionCell继承自NSCell，有关Tracking的方法都在NSCell中，<br>
所以我们完全可以通过重写Tracking的相关方法来自定义Slider的点击、拖拽...</p>
</blockquote>
<pre><code class="language-swift">@objc protocol DNSliderCellDelegate {
    // 我这里定义的都是可选代理，代理者可根据需求去实现方法 
    // 开始拖动
    @objc optional func startTracking(doubleValue:Double ,sender:DNSliderCell)
    // 正在拖动
    @objc optional func continueTracking(doubleValue:Double ,sender:DNSliderCell)
    // 拖动结束 &amp; 点击结束
    @objc optional func stopTracking(doubleValue:Double ,sender:DNSliderCell)
}

extension DNSliderCell {
    //MARK: 开始拖动
    override func startTracking(at startPoint: NSPoint, in controlView: NSView) -&gt; Bool {
        delegate?.startTracking?(doubleValue: doubleValue, sender: self)
        return super.startTracking(at: startPoint, in: controlView)
    }
    //MARK: 拖动中
    override func continueTracking(last lastPoint: NSPoint, current currentPoint: NSPoint, in controlView: NSView) -&gt; Bool {
        delegate?.continueTracking?(doubleValue: doubleValue, sender: self)
        return super.continueTracking(last: lastPoint, current: currentPoint, in: controlView)
    }
    //MARK: 结束拖动 &amp; 点击结束
    override func stopTracking(last lastPoint: NSPoint, current stopPoint: NSPoint, in controlView: NSView, mouseIsUp flag: Bool) {
        delegate?.stopTracking?(doubleValue: doubleValue, sender: self)
        super.stopTracking(last: lastPoint, current: stopPoint, in: controlView, mouseIsUp: flag)
    }
}
</code></pre>
<h3 id="2-定义属性">2. 定义属性</h3>
<blockquote>
<p>可以根据需求自己定义常量或者变量，<code>需要注意</code>的是，Swift5、macOS Catalina 10.15.4上，xib创建的NSSlider会向两边各延伸2个px（其他版本是否如此不确定），例如我自己定义长度1000的slider，实际上长度为1004，所以定义了个Offset方便计算。</p>
</blockquote>
<pre><code class="language-swift">class DNSliderCell: NSSliderCell {
    let Offset:CGFloat = 2 // 进度条，实际上向（根据slider方向）左/右 或者 上/下 偏移2
    let progressColor = kRedHighlightColor // 进度条进度背景色
    var backgroundColor = NSColor.clear // 进度条整体背景色
    let knobColor = kRedHighlightColor // 按钮颜色
    let sliderThickness:CGFloat = 3.0 // 进度条厚度
    let sliderBarRadius:CGFloat = 0 // 进度条圆角
    let KnobWidth:CGFloat = 12.0 // 按钮宽度
    let KnobHeight:CGFloat = 12.0 // 按钮高度
    var progressRect = NSRect() // 进度的Rect
    var needControlKnobHidden = false // 是否需要控制旋钮显示隐藏
    
    weak var delegate:DNSliderCellDelegate? // 代理
    
    // slider是否是竖直
    lazy var isSliderVertical:Bool = {
        let isVertical = (controlView as? DNSlider)?.isVertical == true
        return isVertical
    }()
}
</code></pre>
<h3 id="3-重绘slider">3. 重绘slider</h3>
<blockquote>
<p>要根据其Slider是横着还是竖着的来计算不同的rect</p>
</blockquote>
<pre><code class="language-swift">extension DNSliderCell {
    //MARK: - 重绘slider
    override func drawBar(inside rect: NSRect, flipped: Bool) {
        //MARK: 修改进度条'整体'背景色
        // 1. 获取 slider 的 rect
        var sliderRect = rect
        // 2. 更改 slider 厚度
        if isSliderVertical {
            sliderRect.size.width = sliderThickness
        } else {
            sliderRect.size.height = sliderThickness
        }
        // 3. 填充背景色
        let background = NSBezierPath(roundedRect: sliderRect, xRadius: sliderBarRadius, yRadius: sliderBarRadius)
        self.backgroundColor.setFill()
        background.fill()
        
        //MARK: 修改进度条'进度'背景色
        // 1. 计算当前进度的占有比例
        let value:CGFloat = CGFloat((doubleValue - minValue) / (maxValue - minValue))
        // 2. 根据比例计算进度应有的长度
        var viewLength:CGFloat
        if isSliderVertical {
            viewLength = (controlView?.frame.size.height)!
        } else {
            viewLength = (controlView?.frame.size.width)!
        }
 
        let finalLength:CGFloat = value * (viewLength - Offset * 2)
        // 3. 计算进度的Rect
        progressRect = sliderRect
        if isSliderVertical {
            progressRect.size.height = finalLength
            progressRect.origin.y = Offset
        } else {
            progressRect.size.width = finalLength
            progressRect.origin.x = Offset
        }
        
        // 4. 填充进度背景色
        let active = NSBezierPath(roundedRect: progressRect, xRadius: sliderBarRadius, yRadius: sliderBarRadius)
        self.progressColor.setFill()
        active.fill()
    }
}
</code></pre>
<h3 id="4-重绘旋钮">4. 重绘旋钮</h3>
<blockquote>
<p>要根据其Slider是横着还是竖着的来计算不同的rect</p>
</blockquote>
<pre><code class="language-swift">//MARK: - 重绘旋钮
    override func drawKnob() {
        // 根据比例计算进度应有的OriginX
        let value:CGFloat = CGFloat((doubleValue - minValue) / (maxValue - minValue))
        var viewLength:CGFloat
        if isSliderVertical{
            viewLength = (controlView?.frame.size.height)!
        } else {
            viewLength = (controlView?.frame.size.width)!
        }
        
        var customKnobRect:NSRect
        
        if isSliderVertical {
            //                   Offset +  进度  * ((       实际宽度     ) -  knob宽度 )
            let finalStart:CGFloat = Offset + value * (viewLength - Offset * 2 - KnobHeight)
            // 计算Knob的Rect
            customKnobRect = NSRect(x: progressRect.origin.x + progressRect.size.width / 2 - KnobWidth / 2, y: finalStart, width: KnobWidth , height: KnobHeight)
        } else {
            //                   Offset +  进度  * ((       实际宽度      ) -  knob宽度 )
            let finalStart:CGFloat = Offset + value * (viewLength - Offset * 2 - KnobWidth)
            // 计算Knob的Rect
            customKnobRect = NSRect(x: finalStart, y: progressRect.origin.y + progressRect.size.height / 2 - KnobHeight / 2, width: KnobWidth , height: KnobHeight)
        }
        
        
        // 填充Knob的颜色
        let background = NSBezierPath(roundedRect: customKnobRect, xRadius: KnobWidth / 2, yRadius: KnobHeight / 2)
        if needControlKnobHidden == true {
            if (controlView as? DNSlider)?.shouKnob == true {
                self.knobColor.setFill()
            } else {
                NSColor.clear.setFill()
            }
        } else {
            self.knobColor.setFill()
        }

        background.fill()
    }
</code></pre>
<h2 id="自定义nsslider">自定义NSSlider</h2>
<blockquote>
<p>自定义NSSlider，可以处理鼠标滑过时鼠标指针的状态、控制knob的显示隐藏等; 不废话直接上代码！</p>
</blockquote>
<pre><code class="language-swift">class DNSlider: NSSlider {
    private var trackingArea:NSTrackingArea?
    // 是否显示knob
    var shouKnob:Bool = false    
}

extension DNSlider {
    override func updateTrackingAreas() {
        super.updateTrackingAreas()
        if trackingArea != nil {
            self.removeTrackingArea(trackingArea!)
        }
        
        // 将设置追踪区域为控件大小
        // 设置鼠标追踪区域，如果不设置追踪区域，mouseEntered和mouseExited会无效
        trackingArea = NSTrackingArea(rect: bounds, options: [.mouseEnteredAndExited, .activeAlways], owner: self, userInfo: nil)
        self.addTrackingArea(trackingArea!)
    }
    
    override func mouseEntered(with event: NSEvent) {
        super.mouseEntered(with: event)
        NSCursor.pointingHand.set()
        /*
            通过设置 isHighlighted 取反来触发 cell 的 drawKnob 方法;
            通过 shouKnob 来控制是否显示(因为isHighlighted受其他状态影响，无法精准控制)
         */
        shouKnob = true
        isHighlighted = !isHighlighted
    }
    
    override func mouseExited(with event: NSEvent) {
        super.mouseExited(with: event)
        NSCursor.arrow.set()
        shouKnob = false
        
        // 不直接设置true/false是因为如果在mouseExited前isHighlighted已经是true/false，那就无法触发cell重新渲染了
        isHighlighted = !isHighlighted
    }
    // 点击后，状态会刷新，此时如不做更改会默认改回指针状态
    override func cursorUpdate(with event: NSEvent) {
        super.cursorUpdate(with: event)
        NSCursor.pointingHand.set()
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MacOS关闭按钮与Reopen]]></title>
        <id>https://daning.netlify.app/post/mac-close-and-reopen/</id>
        <link href="https://daning.netlify.app/post/mac-close-and-reopen/">
        </link>
        <updated>2020-07-21T09:29:36.000Z</updated>
        <summary type="html"><![CDATA[<p>在点击Window上的Close按钮后，再在dock栏里重新选中软件，这时软件的各种状态都会重置，一些设置的unowned的地方甚至会crash，这相当于一个半成品的reopen；<code>我们需要的是点击关闭按钮并不会杀掉软件，而是隐藏起来，点击后展示之前软件的显示状态</code>；</p>
]]></summary>
        <content type="html"><![CDATA[<p>在点击Window上的Close按钮后，再在dock栏里重新选中软件，这时软件的各种状态都会重置，一些设置的unowned的地方甚至会crash，这相当于一个半成品的reopen；<code>我们需要的是点击关闭按钮并不会杀掉软件，而是隐藏起来，点击后展示之前软件的显示状态</code>；</p>
<!-- more -->
<h2 id="步骤">步骤</h2>
<p>实现方式很简单，一共就需要两步</p>
<pre><code class="language-swift">//1. 首先定义mainWindow，并在加载后赋值
var mainWindow: NSWindow!
    func applicationDidFinishLaunching(_ aNotification: Notification) {
        mainWindow = NSApplication.shared.windows[0]
    }
</code></pre>
<pre><code class="language-swift">//2. 然后继承NSWindowDelegate，并实现如下方法
extension AppDelegate: NSWindowDelegate {
    func applicationShouldHandleReopen(_ sender: NSApplication, hasVisibleWindows flag: Bool) -&gt; Bool {
        if !flag {
            mainWindow.makeKeyAndOrderFront(nil) // 这里是核心代码
        }
        return false // 这里要设为false，不然会reopen一个窗口
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MacOS自定义TitleBar]]></title>
        <id>https://daning.netlify.app/post/macos-custom-titlebar/</id>
        <link href="https://daning.netlify.app/post/macos-custom-titlebar/">
        </link>
        <updated>2020-07-03T09:28:19.000Z</updated>
        <summary type="html"><![CDATA[<p>简单介绍MacOS开发中如何自定义TitleBar</p>
]]></summary>
        <content type="html"><![CDATA[<p>简单介绍MacOS开发中如何自定义TitleBar</p>
<!-- more -->
<h2 id="默认样式">默认样式</h2>
<p>默认的样式如下，无论是可选中的范围还是样式都比较有局限性</p>
<figure data-type="image" tabindex="1"><img src="https://s1.ax1x.com/2020/09/07/wu5DSO.png" alt="示例" loading="lazy"></figure>
<h2 id="目标示例样式">目标示例样式</h2>
<p>期待的样式如下，选中的范围<code>变高</code>，内容更自由和丰富</p>
<blockquote>
<p>选中范围变大意味着什么？Mac上可以通过拖拽TitleBar来移动整个Window窗口，范围变大则操作更舒服</p>
</blockquote>
<figure data-type="image" tabindex="2"><img src="https://s1.ax1x.com/2020/09/07/wu5g0A.png" alt="示例" loading="lazy"></figure>
<h2 id="思路">思路</h2>
<ul>
<li>NSWindow上的Titlebar可以被隐藏；</li>
<li>NSView可以通过重写mouseDownCanMoveWindow来控制长按移动窗口</li>
</ul>
<h2 id="步骤">步骤</h2>
<ul>
<li>
<p>自定义CustomWindow，继承自NSWindow，然后在Storyboard中将Window的Class改为CustomWindow</p>
</li>
<li>
<p>重新initWithContentRect方法，并隐藏Titlebar，并设置内容铺满整个window（fullSizeContentView）</p>
<pre><code class="language-swift">  class CustomWindow: NSWindow {
      // 个别属性可以在Storyboard里直接设置，这里都用代码写了
      override init(contentRect: NSRect, styleMask style: NSWindow.StyleMask, backing backingStoreType: NSWindow.BackingStoreType, defer flag: Bool) {
          // styleMask 默认是 [.titled, .closable, .miniaturizable, .resizable]
          super.init(contentRect: contentRect,
                     styleMask:[.titled, .closable, .miniaturizable, .resizable, .fullSizeContentView],
                     backing: backingStoreType,
                     defer: flag)
          // titlebar透明
          titlebarAppearsTransparent = true
          // 隐藏title
          titleVisibility = NSWindow.TitleVisibility.hidden
          // 点击窗口背景支持鼠标拖动窗口
          isMovableByWindowBackground = true
          // 设置透明背景
          isOpaque = false
          backgroundColor = NSColor.clear
      }
  }
</code></pre>
</li>
<li>
<p>自定义一个NSView，设定好高度和位置代替原Titlebar</p>
</li>
<li>
<p>此时拖拽窗口的任何位置都可以移动window，那么就要把rootView的mouseDownCanMoveWindow重写为false，CustomTitleBae的mouseDownCanMoveWindow重写为true</p>
<pre><code class="language-swift">override var mouseDownCanMoveWindow: Bool {
    return true; // 该属性默认为true
}
</code></pre>
</li>
<li>
<p>最后记得给rootView设置圆角，不然默认是没有圆角的(我这里直接Storyboard里设置了)<br>
<img src="https://s1.ax1x.com/2020/09/07/wu55p8.png" alt="示例" loading="lazy"></p>
</li>
</ul>
<pre><code class="language-swift">wantsLayer = true
layer.cornerRadius = 7.0
layer.masksToBounds = true
</code></pre>
<h2 id="最终效果">最终效果</h2>
<figure data-type="image" tabindex="3"><img src="https://s1.ax1x.com/2020/09/07/wu5o6g.png" alt="示例" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[获取APP图片资源(破解Assets.car)]]></title>
        <id>https://daning.netlify.app/post/get-assetscar/</id>
        <link href="https://daning.netlify.app/post/get-assetscar/">
        </link>
        <updated>2020-07-03T09:19:26.000Z</updated>
        <summary type="html"><![CDATA[<p>在借鉴(抄袭)或学习一个APP的时候，总是情不自禁的想直接使用人家的图片资源，在拿到iPA或者在MAC下的应用程序里，图片资源一般是<code>Assets.car</code>，这里我们介绍下如何打开这个文件</p>
]]></summary>
        <content type="html"><![CDATA[<p>在借鉴(抄袭)或学习一个APP的时候，总是情不自禁的想直接使用人家的图片资源，在拿到iPA或者在MAC下的应用程序里，图片资源一般是<code>Assets.car</code>，这里我们介绍下如何打开这个文件</p>
<!-- more -->
<h2 id="工具">工具</h2>
<p>cartool：<a href="https://github.com/steventroughtonsmith/cartool">Github地址</a></p>
<h2 id="使用方式">使用方式</h2>
<p>很简单，download到本地后，按照以下步骤，即可直接运行程序获取图片资源:</p>
<ol>
<li>找到 cartool -&gt; Edit Scheme -&gt; Run -&gt; Arguments</li>
<li>在 Ayguments Passed On Launch 配置如下参数：<br>
<img src="https://s1.ax1x.com/2020/09/07/wuyu8A.png" alt="拖拽" loading="lazy"><br>
<em><code>第一个是Assets.car文件路径，第二个是图片文件输出的文件夹路径</code></em><br>
<img src="https://s1.ax1x.com/2020/09/07/wuyMvt.png" alt="参数" loading="lazy"></li>
<li>运行程序，完成破解</li>
</ol>
<h2 id="运行报错">运行报错</h2>
<p><strong>在macOS系统上，有时可能会报错</strong></p>
<figure data-type="image" tabindex="1"><img src="https://s1.ax1x.com/2020/09/07/wuylKP.png" alt="报错" loading="lazy"></figure>
<p><strong>按照以下方式修改即可</strong></p>
<ol>
<li>添加初始化方法<br>
<img src="https://s1.ax1x.com/2020/09/07/wuydvq.png" alt="示例" loading="lazy"></li>
</ol>
<pre><code class="language-objectivec">-(id)initWithURL:(NSURL *)URL error:(NSError **)error;
</code></pre>
<ol start="2">
<li>修改代码<br>
<img src="https://s1.ax1x.com/2020/09/07/wu5SeA.png" alt="示例" loading="lazy"></li>
</ol>
<pre><code class="language-objc">//	CUIThemeFacet *facet = [CUIThemeFacet themeWithContentsOfURL:[NSURL fileURLWithPath:carPath] error:&amp;error];
//
//	CUICatalog *catalog = [[CUICatalog alloc] init];
//
//	/* Override CUICatalog to point to a file rather than a bundle */
//	[catalog setValue:facet forKey:@&quot;_storageRef&quot;];
	
    CUICatalog *catalog = nil;
    if ([CUICatalog instancesRespondToSelector:@selector(initWithURL:error:)]) {
        /* If CUICatalog has the URL API (Mojave), use it. */
        catalog = [[CUICatalog alloc] initWithURL:[NSURL fileURLWithPath:carPath] error:&amp;error];
    } else {
        CUIThemeFacet *facet = [CUIThemeFacet themeWithContentsOfURL:[NSURL fileURLWithPath:carPath] error:&amp;error];
        catalog = [[CUICatalog alloc] init];
        /* Override CUICatalog to point to a file rather than a bundle */
        [catalog setValue:facet forKey:@&quot;_storageRef&quot;];
    }
    NSCAssert(!error, @&quot;Error attempting to open asset catalog (%@): %@&quot;, carPath, error);
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Charles抓包工具介绍]]></title>
        <id>https://daning.netlify.app/post/ho-to-use-charles/</id>
        <link href="https://daning.netlify.app/post/ho-to-use-charles/">
        </link>
        <updated>2020-05-18T09:13:56.000Z</updated>
        <summary type="html"><![CDATA[<p>Charles(青花瓷)是HTTP的抓包工具，可以有效地获取HTTP通信信息；此文章使用的是Charles V4.5.6版本</p>
]]></summary>
        <content type="html"><![CDATA[<p>Charles(青花瓷)是HTTP的抓包工具，可以有效地获取HTTP通信信息；此文章使用的是Charles V4.5.6版本</p>
<!-- more -->
<p>首先展示一下Charles的页面<br>
<img src="https://s1.ax1x.com/2020/09/07/wur5S1.png" alt="示例" loading="lazy"></p>
<h1 id="安装">安装</h1>
<p>安装教程可参考：<a href="http://www.pc6.com/mac/137987.html">链接</a></p>
<p>Charles下载：</p>
<ul>
<li><a href="http://m6.pc6.com/xuh6/Charles456.zip">原下载地址</a></li>
<li><a href="https://pan.baidu.com/s/14yPJssKhyEh2F44BogkxZA">百度云</a> 密码:a999</li>
</ul>
<h2 id="安装步骤">安装步骤</h2>
<ol>
<li>按照上述安装包下载后，即可获取破解版的dmg文件</li>
<li>安装完成后打开Charles，在上方菜单栏的“Help”选择“Register Charles”，会弹出一个Charles for Mac注册界面。<br>
<img src="https://s1.ax1x.com/2020/09/07/wuroy6.png" alt="示例" loading="lazy"></li>
<li>打开解压文件夹中的“keygen.jar”</li>
<li>在运行后的“keygen.jar”上输入“orsoon”然后点击“Generate”生成Charles注册码</li>
<li>然后将生成的charles注册码复制到注册界面的对应位置上，再点击“Register”。<br>
<img src="https://s1.ax1x.com/2020/09/07/wurHeO.png" alt="示例" loading="lazy"></li>
<li>之后会提示注册完成，到这里就可以开始免费使用Charles了。</li>
</ol>
<blockquote>
<p>注意！：在第3步时，可能会提示<code>您需要安装JDK才能使用“java”命令行工具</code>，最简单的安装方式如下：</p>
</blockquote>
<pre><code class="language-shell">brew cask install oracle-jdk
</code></pre>
<p>当然也可以去<a href="https://www.oracle.com/java/technologies/javase-downloads.html">官网</a>下载JDK</p>
<h1 id="使用方式">使用方式</h1>
<p>在此介绍一下如何用Charles抓取手机上的https请求; 参考：<a href="https://www.jianshu.com/p/71dcc417c95a">链接</a></p>
<h2 id="第一步安装charles证书到mac">第一步：安装Charles证书到Mac</h2>
<ol>
<li>启动Charles，选择Help--&gt;SSL Proxying→Install Charles Root Certificate，此时会自动在钥匙串里安装证书</li>
<li>在钥匙串中找到证书Charles Proxy CA，然后双击或者右键显示简介，点开<code>信任</code>选项，选择<code>始终信任</code><br>
<img src="https://s1.ax1x.com/2020/09/07/wurqTe.png" alt="示例" loading="lazy"></li>
<li>移动复制该证书到“系统”里面。此时电脑端的证书就配置完毕了！</li>
</ol>
<h2 id="第二步设置https端口抓包">第二步：设置HTTPS端口抓包</h2>
<ol>
<li>点击Charles--&gt;Proxy--&gt;SSL Proxying Settings，点击&quot;Enable SSL Proxying&quot;,点击“Add”</li>
<li>Host填通配符 <code>*</code> ，Port填通用端口 <code>443</code><br>
<img src="https://s1.ax1x.com/2020/09/07/wurXYd.png" alt="示例" loading="lazy"></li>
</ol>
<h2 id="第三步安装charles证书到手机">第三步：安装Charles证书到手机</h2>
<ol>
<li>首先查看电脑IP地址，点击Charles--&gt;Help→Local IP Address；<br>也可以在网络偏好设置里看；<br>也可以命令行 <code>fconfig en0</code>查看;</li>
<li>手机必须和电脑在同一个Wi-Fi下，然后到wifi详情里，选择配置代理</li>
<li>将代理模式改为<code>手动</code>，填入对应<code>ip</code>，端口号默认<code>8888</code><br>
<img src="https://s1.ax1x.com/2020/09/07/wurzlt.png" alt="示例" loading="lazy"></li>
<li>此时会弹出提示，点击<code>Allow</code><br>
<img src="https://s1.ax1x.com/2020/09/07/wusS6P.png" alt="示例" loading="lazy"></li>
<li>然后打开Safari浏览器，访问：chls.pro/ssl，下载证书<br>
<img src="https://s1.ax1x.com/2020/09/07/wusEfs.png" alt="示例" loading="lazy"></li>
<li>然后再设置--&gt;通用→描述文件，安装刚下载好的文件<br>
<img src="https://s1.ax1x.com/2020/09/07/wus3tJ.png" alt="示例" loading="lazy"></li>
<li>安装完成后，点击 设置--&gt;通用→关于本机，下拉到底部，点击证书 信任设置，把刚信任开关打开。<br>
<img src="https://s1.ax1x.com/2020/09/07/wus8h9.png" alt="示例" loading="lazy"></li>
<li>大功告成，此时你就可以愉快的抓HTTPS的包了。</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[GitBook常用插件]]></title>
        <id>https://daning.netlify.app/post/gitbook-plugins/</id>
        <link href="https://daning.netlify.app/post/gitbook-plugins/">
        </link>
        <updated>2020-05-18T09:04:21.000Z</updated>
        <content type="html"><![CDATA[<p>整理一下目前发现的GitBook的插件（plugins），各插件具体使用方式请看其对应的官方介绍</p>
<h2 id="1-时序图">1. 时序图</h2>
<blockquote>
<p>插件：<strong><code>js-sequence-diagram-full</code></strong><br><br>
说明：时序图样式插件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-js-sequence-diagram-full">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;js-sequence-diagram-full@&gt;=0.3.1&quot;] 
</code></pre>
<ul>
<li>书写方式：</li>
</ul>
<pre><code class="language-shell">    （```sequence）// 忽略括号
        participant A
        participant B

        Note over C: Note over C
        Note over A: Note over A

        C-&gt;D: C-&gt;D
        D-&gt;B: D-&gt;B
        B--&gt;D: B--&gt;D
        D--&gt;C: D--&gt;C
        A-&gt;A: A-&gt;A
    （```）
</code></pre>
<ul>
<li>效果图：<br>
<img src="https://s1.ax1x.com/2020/09/07/wuDVEQ.png" alt="效果图" loading="lazy"></li>
</ul>
<hr>
<h2 id="2-代码块">2. 代码块</h2>
<blockquote>
<p>插件：<strong><code>prism</code></strong><br><br>
说明：代码块的样式插件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-prism">官方介绍</a></p>
</li>
<li>
<p>参数说明：</p>
<ol>
<li>css：代码块样式，官方说明上有几种不同的样式</li>
<li>lang：可以自定义重命名代码块命名，例如 <code>objectivec</code> 有点长，我们就重命名为 <code>oc</code></li>
<li>ignore：忽略其他可能会引起冲突的代码块插件的代码块名字，如 <code>js-sequence-diagram-full</code> 的 <code>sequence</code></li>
</ol>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;prism&quot;]
&quot;pluginsConfig&quot;: {
    &quot;prism&quot;: {
          &quot;css&quot;: [&quot;prismjs/themes/prism-okaidia.css&quot;],
          &quot;lang&quot;: {&quot;oc&quot;: &quot;objectivec&quot;,},
          &quot;ignore&quot;: [&quot;sequence&quot;]
        }
}
</code></pre>
<ul>
<li>效果图：</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://s1.ax1x.com/2020/09/07/wuDnCn.png" alt="效果图" loading="lazy"></figure>
<hr>
<h2 id="3-锚点">3. 锚点</h2>
<blockquote>
<p>插件：<strong><code>anchors</code></strong><br><br>
说明：标题带有 github 样式的锚点。(其实就是鼠标hover在对应h标签后，有个链接小图标显示，点击后标题稍微置顶)</p>
</blockquote>
<blockquote>
<p>如果使用了 <code>anchor-navigation-ex</code>，那就不需要这个<code>anchors</code>插件了，已被其包含</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-anchors">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">  &quot;plugins&quot; : [ &quot;anchors&quot; ]
</code></pre>
<ul>
<li>效果图：</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://s1.ax1x.com/2020/09/07/wuDKg0.png" alt="效果图" loading="lazy"></figure>
<hr>
<h2 id="4-目录宽度拖拽">4. 目录宽度拖拽</h2>
<blockquote>
<p>插件：<strong><code>splitter</code></strong><br><br>
说明：在左侧目录和右侧内容之间添加一个可以拖拽的栏，用来调整两边的宽度。</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-splitter">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">  &quot;plugins&quot; : [ &quot;splitter&quot; ]
</code></pre>
<ul>
<li>效果图：</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://s1.ax1x.com/2020/09/07/wuDMvV.png" alt="效果图" loading="lazy"></figure>
<hr>
<h2 id="5-gtalk-评论">5. gtalk 评论</h2>
<blockquote>
<p>插件：<strong><code>gtalk</code></strong><br><br>
说明：添加gitalk评论插件</p>
</blockquote>
<ul>
<li><a href="https://www.npmjs.com/package/gitbook-plugin-gtalk">官方介绍</a></li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;gtalk&quot;]
&quot;pluginsConfig&quot;: {
      &quot;gtalk&quot;: {
        &quot;clientID&quot;: &quot;GitHub Application Client ID&quot;,
        &quot;clientSecret&quot;: &quot;GitHub Application Client Secret&quot;,
        &quot;repo&quot;: &quot;GitHub repo&quot;,
        &quot;owner&quot;: &quot;GitHub repo owner&quot;,
        &quot;admin&quot;: [&quot;GitHub repo owner and collaborators, only these guys can initialize github issues&quot;]
      }
}
</code></pre>
<hr>
<h2 id="6-github">6. github</h2>
<blockquote>
<p>插件：<strong><code>github</code></strong><br><br>
说明：在右上角显示 github 仓库的图标链接。</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-github">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [ &quot;github&quot; ]
&quot;pluginsConfig&quot;: {
    &quot;github&quot;: {
        &quot;url&quot;: &quot;https://github.com/your/repo&quot;
    }
}
</code></pre>
<hr>
<h2 id="7-章节目录">7. 章节目录</h2>
<blockquote>
<p>插件：<strong><code>anchor-navigation-ex</code></strong><br><br>
说明：目录插件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-anchor-navigation-ex">官方介绍</a></p>
</li>
<li>
<p><a href="https://github.com/zq99299/gitbook-plugin-anchor-navigation-ex/blob/master/doc/effectScreenshot.md">具体参数介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [
   &quot;anchor-navigation-ex&quot;
]
&quot;pluginsConfig&quot;: {
    &quot;anchor-navigation-ex&quot;: {
          &quot;showGoTop&quot;:true, #右下角是否添加“返回顶部”按钮
          &quot;showLevel&quot;: false, #标题是否显示层级序号（控制页面标题是否被重写）
          &quot;printLog&quot;: true, #是否打印处理日志,在排查生成book失败的时候很有用
          &quot;mode&quot;: &quot;float&quot;, #目录的显示模式，目前有三种“float悬浮”、“pageTop文章顶部”、“”不显示，默认float
          &quot;float&quot;: { #对应floatMode的图标，可以设置目录里标题前面的ICON
              &quot;floatIcon&quot;: &quot;fa fa-navicon&quot;,
              &quot;showLevelIcon&quot;: true,
              &quot;level1Icon&quot;: &quot;fa fa-hand-o-right&quot;,
              &quot;level2Icon&quot;: &quot;fa fa-hand-o-right&quot;,
              &quot;level3Icon&quot;: &quot;fa fa-hand-o-right&quot;
          }
    }
}
</code></pre>
<hr>
<h2 id="8-book目录折叠">8. book目录折叠</h2>
<blockquote>
<p>插件：<strong><code>expandable-chapters-small</code></strong><br><br>
说明：左侧的主目录折叠插件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-expandable-chapters-small">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;expandable-chapters-small&quot;] 
</code></pre>
<ul>
<li>效果图：</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://s1.ax1x.com/2020/09/07/wuDNCR.png" alt="效果图" loading="lazy"></figure>
<hr>
<h2 id="9-访问数量">9. 访问数量</h2>
<blockquote>
<p>插件：<strong><code>pageview-count</code></strong><br><br>
说明：文档页面阅读数插件，菜单栏增加个小眼睛，显示访问次数</p>
</blockquote>
<p>PS: 经查询，是访问 <code>https://hitcounter.pythonanywhere.com/count/文章地址</code> 来查询访问次数的</p>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-pageview-count">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;pageview-count&quot;] 
</code></pre>
<ul>
<li>效果图：</li>
</ul>
<figure data-type="image" tabindex="5"><img src="https://s1.ax1x.com/2020/09/07/wuD0KK.png" alt="效果图" loading="lazy"></figure>
<hr>
<h2 id="10-多语言国际化">10. 多语言国际化</h2>
<blockquote>
<p>插件：<strong><code>language-picker</code></strong><br><br>
说明：语言切换插件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-language-picker">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;language-picker&quot;]
&quot;pluginsConfig&quot;: { 
        &quot;language-picker&quot;: { #可以不设置，默认两种
            &quot;grid-columns&quot;: 2 #如果想设置更多种语言，就改这里即可
        }
    }
</code></pre>
<ul>
<li>使用细节，以中/英文2种语言为例:
<ol>
<li>
<p>使用此插件后，GitBook的目录结构需要同时调整，删除以前的SUMMARY</p>
</li>
<li>
<p>创建<code>en</code>、<code>zh-hans</code>2个文件夹，分别创建其对应的<code>README.md</code>和<code>SUMMARY.md</code></p>
</li>
<li>
<p>根目录创建<code>LANGS.md</code>，指定中/英文文档入口，例如</p>
<pre><code class="language-markdown">   * [English](en/)
   * [简体中文](zh-hans/)
</code></pre>
</li>
<li>
<p>这样该gitbook的初始页面其实就变成了<code>LANGS.md</code>，并同时提供了中/英文文档入口。例如：</p>
<figure data-type="image" tabindex="6"><img src="https://s1.ax1x.com/2020/09/07/wurlRI.png" alt="效果图" loading="lazy"></figure>
</li>
</ol>
</li>
</ul>
<hr>
<h2 id="11-插入自定义cssjs">11. 插入自定义css/js</h2>
<blockquote>
<p>插件：<strong><code>addcssjs</code></strong><br><br>
说明：给gitbook添加外部的css、js文件</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.npmjs.com/package/gitbook-plugin-addcssjs">官方介绍</a></p>
</li>
<li>
<p>示例：</p>
</li>
</ul>
<pre><code class="language-json">&quot;plugins&quot;: [&quot;addcssjs&quot;]
&quot;pluginsConfig&quot;: { 
        &quot;addcssjs&quot;: {
            &quot;css&quot;: [&quot;styles/website.css&quot;],
            &quot;js&quot;: [&quot;styles/website.js&quot;]
        }
    }
</code></pre>
<blockquote>
<p>如果你使用了<code>language-picker</code>，那么在<code>pluginsConfig</code>配置路径时，记得要在路径前加上<code>../</code>;<br>因为国际化后，所有book的html已经不在根目录的上了，而是在对应的语言文件夹下面</p>
</blockquote>
<hr>
<h2 id="12-代码块行号-复制">12. 代码块行号、复制</h2>
<blockquote>
<p>插件：<strong><code>code</code></strong><br><br>
说明：为代码块添加行号和复制按钮，复制按钮可关闭</p>
</blockquote>
<ul>
<li><a href="https://www.npmjs.com/package/gitbook-plugin-code">官方介绍</a></li>
</ul>
<pre><code class="language-json">{
    &quot;plugins&quot; : [ 
            &quot;code&quot; 
     ],
    &quot;pluginsConfig&quot;: {
      &quot;code&quot;: {
        &quot;copyButtons&quot;: false #复制按钮可关闭
      }
    }
}
</code></pre>
]]></content>
    </entry>
</feed>